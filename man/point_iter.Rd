% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Single_Iteration_EM Algorithm.R
\name{point_iter}
\alias{point_iter}
\title{Perform a Single Iteration of the Composite EM Algorithm}
\usage{
point_iter(beta, lambda, l, u, u.star, tau, x, risk, weights = NULL)
}
\arguments{
\item{beta}{Numeric vector. Current estimates of the fixed effect coefficients, of length \code{p}.}

\item{lambda}{List. Current estimates of the baseline hazard functions, one vector \code{lambda.s} for each stratum.}

\item{l}{List. Left endpoints \code{l.s} of the censoring intervals for each stratum.}

\item{u}{List. Right endpoints \code{u.s} of the censoring intervals for each stratum.}

\item{u.star}{List. Modified upper bounds \code{u.s.star} with \code{Inf} replaced for computational convenience.}

\item{tau}{List. Sorted unique time points \code{tau.s} for each stratum.}

\item{x}{List. Covariate arrays \code{x.s} for each stratum.}

\item{risk}{List. Binary matrices \code{risk.vr} indicating risk set membership for each observation and time point.}

\item{weights}{Optional list. Perturbation weights \code{weight.s} for each stratum, used in bootstrap variance estimation.}
}
\value{
A list containing:
\itemize{
\item \code{beta}: Updated coefficient estimates after one EM iteration.
\item \code{lambda}: Updated baseline hazard estimates after one EM iteration.
}
}
\description{
Executes one iteration of the composite Expectation-Maximization (EM) algorithm
for estimating the regression parameters and baseline hazard functions in marginal
Cox regression models with clustered interval-censored data.
}
\seealso{
\code{\link{composite_coxIC}}, \code{\link{em_init}}, \code{\link{update_beta}}, \code{\link{update_lambda}}
}
