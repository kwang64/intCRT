% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Composite Log-Likelihood Functions.R
\name{composite_i_1}
\alias{composite_i_1}
\title{Computes the composite log-likelihood contribution for a cluster (when clustering and stratification coincide)}
\usage{
composite_i_1(beta, lambda.i, l.i, u.i, tau.i, x.i)
}
\arguments{
\item{beta}{Numeric vector. Current estimates of the fixed effect coefficients (length \eqn{p}).}

\item{lambda.i}{Numeric vector of length \eqn{\rho_i}. Estimated baseline hazard function for cluster (stratum) \eqn{i}.}

\item{l.i}{Numeric vector of length \eqn{n_i}. Left endpoints \eqn{L_{ij}} of censoring intervals for individuals in cluster \eqn{i}.}

\item{u.i}{Numeric vector of length \eqn{n_i}. Right endpoints \eqn{U_{ij}} of censoring intervals for individuals in cluster \eqn{i}.}

\item{tau.i}{Numeric vector of length \eqn{\rho_i}. Sorted unique timepoints \eqn{\tau_{ir}} for cluster (stratum) \eqn{i}.}

\item{x.i}{Array of dimension \eqn{n_i \times p \times \rho_i}. Covariate array where \code{x.i[j, , ]} gives a \eqn{p \times \rho_i} matrix for individual \eqn{j} in cluster \eqn{i}.}
}
\value{
Numeric. The composite log-likelihood contribution from cluster \eqn{i}.
}
\description{
Calculates the independence composite log-likelihood contribution of cluster \eqn{i}, assuming the clustering and stratification variable(s) are the same,
under a marginal Cox regression model for interval-censored data.
}
\details{
For each individual \eqn{j} in cluster \eqn{i}, this function extracts the individual covariate matrix, identifies the appropriate interval,
and evaluates the individual composite log-likelihood contribution using \code{\link{composite_ij}}. The total contribution from cluster \eqn{i}
is then calculated as the sum of these individual contributions.
}
\keyword{internal}
