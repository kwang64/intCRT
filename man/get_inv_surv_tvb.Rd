% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0_Data_Generation_CombPrev.R
\name{get_inv_surv_tvb}
\alias{get_inv_surv_tvb}
\title{Invert Individual Survival Curve to Obtain Failure Time}
\usage{
get_inv_surv_tvb(s, baseline.i, bfns, aux, x.i)
}
\arguments{
\item{s}{Numeric. Target survival probability (e.g., 0.5 for median survival).}

\item{baseline.i}{List. Contains stratum-specific baseline functions: \code{survival}, \code{density}, and \code{hazard}, each as data frames with time.}

\item{bfns}{List. A list defining the functional form for the time-varying effect \eqn{\beta(t)}.
Must contain a function \code{beta.t()}.}

\item{aux}{List. Additional arguments required by \code{beta.t()}, such as parameters \code{beta} and possibly \code{tau}.}

\item{x.i}{Numeric. Indicator of intervention assignment (e.g., 0 or 1).}
}
\value{
Numeric. The estimated failure time \eqn{t} such that \eqn{S(t) = s}.
}
\description{
Returns the failure time \eqn{t} corresponding to a specified survival probability \eqn{S(t) = s}
for an individual, based on a time-dependent covariate effect and stratum-specific baseline hazard.
}
\details{
Internally, this function computes the individual survival curve using \code{\link{get_surv_tvb}}, then finds
the earliest time at which the survival curve falls below or equals the target survival probability.
}
\examples{
# Example assuming you already have baseline.i, bfns, aux, and x.i defined:
# median_time <- get_inv_surv_tvb(s = 0.5, baseline.i, bfns, aux, x.i = 1)

}
\seealso{
\code{\link{get_surv_tvb}}
}
