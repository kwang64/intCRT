% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Maximization_Step_EM_Algorithm.R
\name{update_beta}
\alias{update_beta}
\title{Solve the Profile Composite Likelihood Score Equation for \code{beta}}
\usage{
update_beta(beta, u.star, tau, x, w, risk, weights = NULL)
}
\arguments{
\item{beta}{Numeric vector of length \code{p}, the current estimates of fixed effects.}

\item{u.star}{List of length \code{S}, each element a numeric vector of \code{u^*_sv} for stratum \code{s}.}

\item{tau}{List of length \code{S}, each element a numeric vector of unique timepoints \code{tau.s} for stratum \code{s}.}

\item{x}{List of length \code{S}, each element a 3D covariate array \code{x.s} for stratum \code{s}.}

\item{w}{List of length \code{S}, each element a matrix of expected values from the E-step \code{w.s} for stratum \code{s}.}

\item{risk}{List of length \code{S}, each element a matrix of risk set indicators \code{risk.vr} for stratum \code{s}.}

\item{weights}{Optional list of length \code{S}, each element a numeric vector of weights \code{weights.s} for stratum \code{s}.
If \code{NULL}, defaults to equal weights for all individuals.}
}
\value{
A numeric vector of updated \code{beta} values (length \code{p}).
}
\description{
Updates the fixed effects \code{beta} by solving the profile composite likelihood
score equation using a root-finding procedure, with optional perturbation weights.
}
