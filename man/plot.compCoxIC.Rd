% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Summary and Visualization Functions.R
\name{plot.compCoxIC}
\alias{plot.compCoxIC}
\title{Plot Estimated Baseline Cumulative Hazard, Survival, or Cumulative Incidence Functions}
\usage{
\method{plot}{compCoxIC}(
  x,
  type = c("cumulative hazard", "survival", "cumulative incidence"),
  combine = TRUE,
  x.title = "Time",
  legend.title = "Stratum",
  y.title = NULL
)
}
\arguments{
\item{x}{List. Model output from \code{composite_coxIC()}, containing baseline hazard estimates by stratum.}

\item{type}{Character vector. Which baseline distribution function(s) to plot. Options include
\code{"cumulative hazard"}, \code{"survival"}, and \code{"cumulative incidence"}. Can specify multiple.}

\item{combine}{Logical. If \code{TRUE}, plots all strata on a single combined plot. If \code{FALSE},
plots separate plots for each stratum.}

\item{x.title}{Character. Label for the x-axis (default \code{"Time"}).}

\item{legend.title}{Character. Title for the legend indicating strata (default \code{"Stratum"}).}

\item{y.title}{Optional character vector of length matching \code{type} to customize y-axis titles.
If \code{NULL}, default titles will be used.}
}
\value{
NULL. This function prints ggplot2 plots to the current graphics device.
}
\description{
Visualizes the estimated baseline distribution functions from the output of \code{composite_coxIC()}.
Users can plot cumulative hazard, survival, or cumulative incidence functions, either combined on one plot
or separately by stratum.
}
\details{
The function computes cumulative sums of the estimated baseline hazard to derive cumulative hazard,
survival (via \eqn{\exp(-\text{cumulative hazard})}), or cumulative incidence (via \eqn{1-\exp(-\text{cumulative hazard})}).
Multiple types can be plotted by specifying them in \code{type}.
}
